name: Finalize instruction handoff metadata (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This one-time finalizer is obsolete and intentionally disabled."
          echo "Canonical v2 instructions are maintained directly in Drive and mirrored to GitHub/Library/Notion during full checkpoints."
          echo "Routine prompt sync uses sync-from-drive.yml only."
