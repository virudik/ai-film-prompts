name: Legacy split-source builder (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain current architecture
        run: |
          echo "This legacy _source/part-*.md builder is intentionally disabled."
          echo "Canonical flow: Google Drive video-prompts.md -> GitHub root video-prompts.md -> GitHub Pages."
          echo "Use .github/workflows/sync-from-drive.yml or update the canonical Drive master instead."
