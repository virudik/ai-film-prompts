name: Sync instruction status metadata (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This one-time timestamp patcher is disabled. Full-sync timestamps must be updated consistently across Drive master, instruction copies, Library, GitHub and Notion by the active editor following SYNC-RUNBOOK.md."
